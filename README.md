# 중고 물품 통합 검색 🔍

중고나라와 번개장터를 한 번에 검색할 수 있는 웹 기반 통합 검색 서비스입니다.

## ✨ 주요 기능

### 🔄 통합 검색

- **번개장터 + 중고나라 동시 검색**: 한 번의 검색으로 두 플랫폼 모두 조회
- **최신순 자동 정렬**: 두 플랫폼 결과를 최신 등록순으로 통합 정렬
- **실시간 검색**: 최신 판매 상품만 필터링하여 표시

### 📄 페이징 시스템

- **페이지당 50개 항목**: 읽기 편한 분량으로 결과 표시
- **직관적인 페이지 이동**: 처음/이전/다음/마지막 버튼 제공
- **자동 스크롤**: 페이지 이동 시 자동으로 상단 이동

### 🎨 사용자 경험

- **반응형 디자인**: 모바일/태블릿/데스크톱 최적화
- **SEO 최적화**: 검색 엔진 노출을 위한 메타데이터 설정
- **빠른 로딩**: 클라이언트 사이드 페이징으로 빠른 탐색

## 🛠️ 기술 스택

```json
{
  "backend": {
    "runtime": "Node.js",
    "framework": "Express.js",
    "libraries": {
      "axios": "HTTP 요청 (중고나라 API)",
      "cheerio": "HTML 파싱",
      "dotenv": "환경 변수 관리"
    }
  },
  "frontend": {
    "html": "시맨틱 HTML5",
    "css": "반응형 CSS Grid",
    "javascript": "바닐라 JS (프레임워크 없음)"
  },
  "deployment": "Vercel (serverless)"
}
```

## 📂 프로젝트 구조

```
coolman/
├── src/
│   ├── index.js      # Express 서버 및 API 엔드포인트
│   ├── scraper.js    # 번개장터/중고나라 API 스크래퍼
│   └── types.js      # JSDoc 타입 정의
├── public/
│   └── index.html    # 검색 웹 인터페이스
├── config/
│   └── config.js     # 설정 파일
├── .env              # 환경 변수 (PORT)
├── vercel.json       # Vercel 배포 설정
└── package.json      # 프로젝트 메타데이터
```

## 🚀 빠른 시작

### 1. 저장소 클론 및 패키지 설치

```bash
git clone git@github.com:cozynye/coolman.git
cd coolman
npm install
```

### 2. 환경 변수 설정 (선택사항)

`.env` 파일을 생성하여 포트 변경 가능:

```env
PORT=3010
```

### 3. 서버 실행

```bash
npm start
```

서버가 `http://localhost:3010`에서 실행됩니다.

### 4. 웹 브라우저에서 접속

```
http://localhost:3010
```

검색창에 키워드를 입력하고 검색하세요! (예: "맥북", "아이폰", "아이패드")

## 🔌 API 사용법

### POST /api/search

특정 키워드로 번개장터와 중고나라를 검색합니다.

**요청:**

```bash
curl -X POST http://localhost:3010/api/search \
  -H "Content-Type: application/json" \
  -d '{"keyword": "맥북"}'
```

**응답:**

```json
{
  "keyword": "맥북",
  "timestamp": "2025-12-16T07:30:00.000Z",
  "results": [
    {
      "platform": "번개장터",
      "title": "맥북 에어 M2 13인치",
      "price": "900,000원",
      "link": "https://bunjang.co.kr/products/123456",
      "update_time": "2025. 12. 16. 오후 4:15:30",
      "timestamp": 1734343530,
      "status": "판매중"
    },
    {
      "platform": "중고나라",
      "title": "맥북프로 14인치 M3",
      "price": "2,200,000원",
      "link": "https://m.joongna.com/product/223003262",
      "update_time": "2025. 12. 16. 오후 3:05:48",
      "timestamp": 1734339948,
      "status": "판매중",
      "location": "서울"
    }
  ]
}
```

### GET /api/test-raw

번개장터 API 직접 테스트용 엔드포인트입니다.

```bash
curl "http://localhost:3010/api/test-raw?keyword=맥북"
```

## 📊 검색 동작 원리

### 1. 검색 플로우

```
사용자 키워드 입력
    ↓
번개장터 + 중고나라 병렬 검색 (Promise.all)
    ↓
각 플랫폼별 필터링
    ├─ 번개장터: 24시간 이내 + 판매중 + 광고 제외
    └─ 중고나라: 판매완료 제외
    ↓
결과 통합 및 timestamp 기준 최신순 정렬
    ↓
클라이언트로 전송 (최대 ~100개)
    ↓
클라이언트에서 50개씩 페이징 처리
```

### 2. 번개장터 API

```javascript
// 엔드포인트
https://api.bunjang.co.kr/api/1/find_v2.json

// 필터링 조건
- type === 'PRODUCT' (광고 제외)
- status === '0' (판매중)
- update_time > 24시간 전 (최신 매물)
```

### 3. 중고나라 API

```javascript
// 엔드포인트
https://search-api.joongna.com/v3/search/app

// 페이로드
{
  "searchWord": "검색키워드",
  "saleYn": "SALE_N",  // 판매완료 제외
  "quantity": 50,
  "sort": "RECOMMEND_SORT"
}
```

## 🌐 Vercel 배포

### 자동 배포 (GitHub 연동)

1. Vercel 계정에 GitHub 연결
2. `coolman` 저장소 import
3. 자동으로 배포 완료!

### 수동 배포

```bash
npm install -g vercel
vercel --prod
```

배포 후 제공되는 URL로 접속하면 바로 사용 가능합니다.

## 🎯 사용 예시

### 시나리오: 맥북 중고 매물 찾기

1. 웹사이트 접속
2. 검색창에 "맥북" 입력
3. 번개장터 + 중고나라 최신 매물 ~100개 확인
4. 페이지 이동하며 가격/상태 비교
5. 마음에 드는 매물 클릭하여 상세 페이지 이동

### 검색 팁

- **구체적인 모델명**: "맥북 에어 M2" > "맥북"
- **브랜드 + 제품**: "애플 아이패드 프로"
- **시리즈명**: "아이폰 15 프로"

## 🔒 보안 및 주의사항

### Git에 커밋하지 말아야 할 파일

```gitignore
.env              # 환경 변수 (포트 설정 등)
node_modules/     # 패키지 의존성
.vercel/          # Vercel 배포 설정
```

모든 민감한 정보는 `.gitignore`에 이미 추가되어 있습니다.

## 📈 향후 개선 사항

- [ ] 가격 범위 필터 추가
- [ ] 지역별 필터링
- [ ] 북마크/찜 기능
- [ ] 검색 히스토리 저장
- [ ] 알림 설정 (새 매물 알림)
- [ ] 더 많은 중고 플랫폼 추가 (당근마켓 등)

## 🐛 트러블슈팅

### 서버가 시작되지 않을 때

```bash
# 3010 포트 사용 중인 프로세스 확인
lsof -ti:3010

# 프로세스 종료
kill $(lsof -ti:3010)
```

### 검색 결과가 나오지 않을 때

1. API 서버 상태 확인 (번개장터/중고나라 API 변경 가능성)
2. 네트워크 연결 확인
3. 브라우저 콘솔에서 에러 확인

### 페이지가 로드되지 않을 때

1. 브라우저 캐시 삭제 (Ctrl+Shift+R / Cmd+Shift+R)
2. 시크릿 모드로 접속 시도
3. 서버 재시작

## 📝 라이선스

ISC

## 🙏 감사의 말

이 프로젝트는 중고 물품을 찾는 사용자들의 편의를 위해 만들어졌습니다.
개선 제안이나 버그 리포트는 GitHub Issues를 통해 제출해주세요.

---

**Last Updated**: 2025-12-16
